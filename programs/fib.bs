function fib(n: Int) {
  var fiba = 0
  var fibb = 1
  var fibonacci = fibb
  for (var i = 0; i < n; i++) {
    print(string(fibonacci))
    fibonacci = fiba + fibb
    fiba = fibb
    fibb = fibonacci
  }
}